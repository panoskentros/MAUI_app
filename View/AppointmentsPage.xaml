<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:MAUI_app.View"
             x:Class="MAUI_app.View.AppointmentsPage"
             BackgroundColor="#F4F6F8"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">
        <view:HeaderBanner x:Name="PageBanner" Grid.Row="0" />

        <VerticalStackLayout Grid.Row="1" Padding="20" Spacing="15">
            <Label Text="All Scheduled Appointments" 
                   FontSize="22" 
                   FontAttributes="Bold" 
                   TextColor="#2C3E50" />

            <CollectionView x:Name="AllAppointmentsList" ItemsSource="{Binding DailyAppointments}">
                
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.EmptyView>
                    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Margin="0,50">
                        <Label Text="No appointments found in database." HorizontalTextAlignment="Center" TextColor="Gray"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" BackgroundColor="White" StrokeThickness="0" Padding="15">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="16" />
                            </Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow Brush="Black" Offset="0,4" Radius="10" Opacity="0.05" />
                            </Border.Shadow>

                            <Grid ColumnDefinitions="80, *, Auto" ColumnSpacing="15">
                                
                                <Border Grid.Column="0" BackgroundColor="#F4F6F8" StrokeThickness="0" Padding="10" VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="12" />
                                    </Border.StrokeShape>
                                    <VerticalStackLayout HorizontalOptions="Center" Spacing="2">
                                        <Label Text="{Binding AppointmentDate, StringFormat='{0:MMM}'}" FontSize="12" FontAttributes="Bold" TextColor="#0D47A1" HorizontalOptions="Center" TextTransform="Uppercase"/>
                                        <Label Text="{Binding AppointmentDate, StringFormat='{0:dd}'}" FontSize="22" FontAttributes="Bold" TextColor="#1C1C1E" HorizontalOptions="Center"/>
                                        <Label Text="{Binding AppointmentDate, StringFormat='{0:HH:mm}'}" FontSize="12" TextColor="#7F8C8D" HorizontalOptions="Center" FontAttributes="Bold"/>
                                    </VerticalStackLayout>
                                </Border>

                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="5">
                                    <Label Text="{Binding PatientName}" FontSize="18" FontAttributes="Bold" TextColor="#2C3E50" />
                                    
                                    <HorizontalStackLayout Spacing="6">
                                        <BoxView Color="#BDC3C7" WidthRequest="4" HeightRequest="4" CornerRadius="2" VerticalOptions="Center"/>
                                        <Label Text="{Binding MedicalNotes, TargetNullValue='Standard Checkup'}" FontSize="14" TextColor="#7F8C8D" VerticalOptions="Center" LineBreakMode="TailTruncation" MaxLines="1"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>

                                <Border Grid.Column="2" BackgroundColor="#E8F8F5" StrokeThickness="0" Padding="15,8" VerticalOptions="Center" HorizontalOptions="End">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Label Text="{Binding Status}" FontSize="12" FontAttributes="Bold" TextColor="#27AE60" TextTransform="Uppercase" />
                                </Border>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </Grid>
</ContentPage>